name: Runner Canary
run-name: "Introspect Runner on ${{ github.repository }} by ${{ github.actor }} with ${{ github.event_name }} (Attempt ${{ github.run_attempt }})"

on:
  push:
    branches-ignore:
      - main
  workflow_dispatch:

jobs:
  canary:
    uses: Mimis-Gildi/fluffle/.github/workflows/custom-runner-introspect.yml@67-add-release-shared-org-actions
